# Placeholder — add provider resources for the optionally-hosted backend
# here (container registry, Kubernetes cluster, etc.) once you've picked a
# cloud provider in versions.tf. GrafeoDB is embedded (in-process), so no
# managed database resource is needed here.
