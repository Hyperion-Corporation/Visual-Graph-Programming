name: Agent Backlog Sync

# Keeps the ProjectV2 board in sync with ROADMAP.md / CHANGELOG.md by
# orchestrating github/scripts/sync_backlog.py. Runs on pushes to main that
# touch either source-of-truth doc, or on demand via workflow_dispatch for
# maintainers who want to force a resync without a doc edit.

on:
  push:
    branches: [main]
    paths:
      - "ROADMAP.md"
      - "CHANGELOG.md"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Print the proposed diff without mutating the board"
        type: boolean
        default: false

permissions:
  contents: read
  issues: write
  pull-requests: write

concurrency:
  group: agent-backlog-sync
  cancel-in-progress: false

jobs:
  sync:
    name: Sync backlog to Project board
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: pyproject.toml

      - name: Install automation dependencies
        run: pip install --require-hashes=false -e ".[ci]"

      - name: Validate automation config
        run: |
          python -c "import json,pathlib; json.loads(pathlib.Path('github/config/project_labels.json').read_text())"
          python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('github/config/automation_rules.yaml').read_text())"

      - name: Run backlog sync
        env:
          GITHUB_TOKEN: ${{ secrets.PROJECT_AUTOMATION_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          VGP_PROJECT_ID: ${{ vars.VGP_PROJECT_ID }}
        run: |
          ARGS="--repo-owner ${{ github.repository_owner }} \
                --repo-name $(basename ${{ github.repository }}) \
                --project-id ${VGP_PROJECT_ID}"
          if [ "${{ inputs.dry_run }}" = "true" ]; then
            ARGS="$ARGS --dry-run"
          fi
          python github/scripts/sync_backlog.py $ARGS

      - name: Summarize run
        if: always()
        run: |
          echo "### Agent Backlog Sync" >> "$GITHUB_STEP_SUMMARY"
          echo "Trigger: ${{ github.event_name }}" >> "$GITHUB_STEP_SUMMARY"
          echo "Dry run: ${{ inputs.dry_run || 'false' }}" >> "$GITHUB_STEP_SUMMARY"
